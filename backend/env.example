# .env.example — Copy this to .env and fill in real values
# NEVER commit .env to GitHub

# Anthropic Claude API key (from console.anthropic.com)
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxx

# JWT secret — generate with:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=replace_with_a_long_random_64char_string

# ── ElevenLabs (Text-to-Speech) ───────────────────────────────────────
# Get your key at: https://elevenlabs.io → Profile → API Keys
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# Voice ID — find yours at: https://elevenlabs.io/voice-library
# Or use the API: GET https://api.elevenlabs.io/v1/voices
# Default below is "Sarah" (professional, clear English voice)
ELEVENLABS_VOICE_ID=EXAVITQu4vr4xnSDxMaL

# Port (Render sets this automatically — leave as 5000 for local dev)
PORT=5000
